<UserControl x:Class="DocSearcher.Control.SelectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="ExpanderTemplate">
        </DataTemplate>
    </UserControl.Resources>

    <ScrollViewer Margin="5 0 0 0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Parameter your research" Grid.Row="0" Grid.ColumnSpan="3"
                       FontWeight="Bold" FontSize="14" Margin="0 0 0 10" />

            <StackPanel Grid.Row="1" Grid.ColumnSpan="3">
                <Expander Header="Documents" Grid.Row="1" Content="{Binding }" />
                <Expander Header="Video" Grid.Row="2" Content="{Binding }" />
                <Expander Header="Images" Grid.Row="3" Content="{Binding }" />
                <Expander Header="Music" Grid.Row="4" Content="{Binding }" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>