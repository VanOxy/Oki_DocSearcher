<UserControl x:Class="DocSearcher.Control.ChartsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:chart="clr-namespace:De.TorstenMandelkow.MetroChart;assembly=De.TorstenMandelkow.MetroChart">
    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <chart:StackedColumnChart x:Name="MyChart" ChartTitle="Stats :" ChartSubTitle="">
            <chart:StackedColumnChart.Series>
                <chart:ChartSeries />
            </chart:StackedColumnChart.Series>
        </chart:StackedColumnChart>

        <!--<WrapPanel HorizontalAlignment="Right" Grid.Row="1">
            <TextBlock Text="Size in : "  Margin="0 0 10 0" />
            <ComboBox ItemsSource="{Binding DataRepresentationList}" SelectedIndex="1"
                      SelectionChanged="ComboBox_SelectionChanged" />
        </WrapPanel>-->
    </Grid>
</UserControl>