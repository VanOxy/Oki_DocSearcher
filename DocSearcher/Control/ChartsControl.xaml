<UserControl x:Class="DocSearcher.Control.ChartsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:chart="clr-namespace:De.TorstenMandelkow.MetroChart;assembly=De.TorstenMandelkow.MetroChart"
             DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
    <Grid>
        <chart:StackedColumnChart ChartTitle="Chart Title"
                                  ChartSubTitle="Chart Sub-Title">
            <chart:StackedColumnChart.Series>
                <chart:ChartSeries ItemsSource="{Binding Stats}"
                                   DisplayMember="DocType"
                                   ValueMember="" />
                <chart:ChartSeries SeriesTitle="{Binding Stats.Type}"
                                   DisplayMember="Category"
                                   ValueMember="Number"
                                   ItemsSource="{Binding Path=Warnings}" />
            </chart:StackedColumnChart.Series>
        </chart:StackedColumnChart>
    </Grid>
</UserControl>